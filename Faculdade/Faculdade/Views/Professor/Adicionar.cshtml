
@{
    ViewBag.Title = "Adicionar";
}

<h2>Adicionar</h2>

<div ng-app="MyApp" ng-controller="ProfessorController">
    <div>
        <label>Nome: </label>
        <input type="text" ng-model="curso.Nome"/>
    </div>
    <div>
        <input type="submit" style="left:90%;" class="btn btn-primary" ng-click="Adicionar(curso)" value="Save" />
    </div>
</div>
